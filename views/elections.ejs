<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./css/style.css">
    <title><%= title %></title>
</head>
<body class="bg-gray-50">
    <div class="my-6 mx-10">
        <h3 class="text-4xl my-4 font-medium">Hi <%= userName %> !</h3>    
            </div>
            <% if (messages.success) { %>
                <p class="bg-purple-100 m-3 list-none rounded-md px-4 py-2"><%= messages.success %></p>
            <% } %>
        </div>
        <div class="text-center">
            <h4>Want to create elections---Try here</h4>
        </div>
        <br>
            <div class="grid grid-cols-8">
                <div class="col-start-4 col-span-2">
            <div class="bg-black rounded px-2 py-1 text-center">
                <a href="/elections/create" class="text-white">Create New Election</a>
            </div>
        </div></div>
        <div class="flex gap-2 py-4 justify-between">
        <h5 class="text-2xl">Your Elections:</h5>
        </div>
        <div class="flex-auto">
            <div class="mx-4">  
                <p class="text-black-600"><%= (elections.length==0)?"You haven't created any elections yet!!!":"" %></p>
                <ol class="list-decimal">
                    <% for (let i=0; i<elections.length; i++) { %>
                        <li>
                            <div class="flex items-center w-fit my-2 px-2 py-1 rounded hover:bg-white">
                                <%= elections[i].electionName %>
                            </div>
                        </li>
                    <% } %>
                </ol>
            </div>
        </div>
    <div><%- include('header.ejs') %></div>
    </div>
</body>
</html>